hl.config({
    input = {
        touchpad = {
            disable_while_typing = true,
        },
    },
})
hl.config({
    input = {
        touchpad = {
            disable_while_typing = false,
        },
    },
})
